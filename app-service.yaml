apiVersion: v1
kind: Service
metadata:
  name: url-shortener-service
  namespace: url-shortener
spec:
  selector:
    app: url-shortener
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
